# npm install tricks with package.json

When doing `npm install`, these flags can be specified to automatically add the package to `dependencies` or `devDependencies` in `package.json`:
- `npm install -S <package>` to add to `dependencies`
- `npm install -D <package>` to add to `devDependencies`

[reference](https://docs.npmjs.com/cli/install)
